# 문제 링크 : https://school.programmers.co.kr/learn/courses/30/lessons/133026

# 내 답안

SELECT INGREDIENT_TYPE , SUM(TOTAL_ORDER) AS TOTAL_ORDER
FROM (SELECT A.SHIPMENT_ID , A.FLAVOR , A.TOTAL_ORDER , B.INGREDIENT_TYPE
FROM FIRST_HALF AS A LEFT JOIN ICECREAM_INFO AS B ON A.FLAVOR = B.FLAVOR) AS C
GROUP BY INGREDIENT_TYPE
ORDER BY TOTAL_ORDER ASC;
